# Errors from a validation report

Errors from a validation report

## Usage

``` r
mp_errors(report)
```

## Arguments

- report:

  An `mp_validation_report` from [`mp_validate()`](mp_validate.md).

## Value

A data frame of the `error`-level findings only.
