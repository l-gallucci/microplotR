% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_mag.R
\name{mp_validate_mag}
\alias{mp_validate_mag}
\title{Validate a MAG (metagenome-assembled genome) quality table}
\usage{
mp_validate_mag(mag_table)
}
\arguments{
\item{mag_table}{A data frame with at least a bin-identifier column
(\code{Name} or CheckM1's \code{"Bin Id"}), \code{Completeness}, \code{Contamination},
and a genome-size column (\code{Genome_Size} or CheckM1's \code{"Genome size (bp)"}).}
}
\value{
An \code{mp_validation_report} (see \code{\link[=mp_validate]{mp_validate()}}).
}
\description{
Checks a single flat table shaped like CheckM2's \code{quality_report.tsv} or
CheckM (v1)'s \code{qa}/\code{bin_stats} table (one row per bin/MAG) -- not the
3-file taxonomy/metadata schema used by \code{\link[=mp_validate]{mp_validate()}}. Column names from
either tool are accepted (e.g. CheckM1's \code{"Bin Id"} or CheckM2's \code{"Name"})
and normalized internally. Mirrors the same \verb{\{level, file, field, message\}} finding shape.
}
