% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette.R
\name{mp_nested_order_palette}
\alias{mp_nested_order_palette}
\title{Build ordering, nested color palette, and nested legend labels for a
taxon-colored plot}
\usage{
mp_nested_order_palette(
  taxa,
  groups,
  totals,
  other_label = "Other",
  other_color = "grey80"
)
}
\arguments{
\item{taxa}{Character vector, one entry per unique taxon (e.g. genus).}

\item{groups}{Character vector, same length as \code{taxa}, the upper taxonomy
level (e.g. phylum) each taxon belongs to.}

\item{totals}{Numeric vector, same length as \code{taxa}, overall abundance
used to rank groups and within-group taxa.}

\item{other_label}{The sentinel value used for the aggregated "everything
else" bucket, if present in \code{taxa}.}

\item{other_color}{Color for \code{other_label}.}
}
\value{
A list with \code{order} (character vector, plotting/legend order),
\code{palette} (named character vector, taxon -> hex color), \code{labels} (named
character vector, taxon -> markdown legend label).
}
\description{
Orders taxa grouped by an upper taxonomy level (each group's taxa kept
contiguous, groups and within-group taxa both ordered by descending
abundance), assigns each group a base hue and shades its member taxa from
that hue (so related taxa read as a family of colors), and builds legend
labels where the first taxon in each group carries a bold \verb{**group**}
markdown header (rendered via \code{ggtext::element_markdown()}).
\code{other_label} (e.g. "Other") is always ordered last and colored grey, with
a plain (non-bold) label.
}
