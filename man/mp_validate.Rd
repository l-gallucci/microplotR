% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{mp_validate}
\alias{mp_validate}
\title{Validate tidy microbiome input (feature table + taxonomy + metadata)}
\usage{
mp_validate(
  data,
  gradient_column = NULL,
  group_column = NULL,
  required_ranks = c("Phylum", "Genus")
)
}
\arguments{
\item{data}{List as returned by \code{\link[=mp_read_data]{mp_read_data()}}, with \code{feature_table},
\code{taxonomy}, \code{metadata} tibbles.}

\item{gradient_column}{Optional metadata column to check is numeric
(required by the ASV-level gradient plot).}

\item{group_column}{Optional metadata column to check has >= 2 levels
(required by alpha/beta diversity group comparisons).}

\item{required_ranks}{Column names that must be present in \code{data$taxonomy}.
Default \code{c("Phylum", "Genus")} for the standard 16S taxonomy shape.
Pass \code{character(0)} when \code{data$taxonomy} is actually a non-taxonomic
hierarchy (e.g. a functional annotation table from eggNOG-mapper/
KofamScan, see \code{\link[=mp_function_barplot]{mp_function_barplot()}}) whose column names aren't ranks.}
}
\value{
An \code{mp_validation_report}: a list with \code{findings}, a data frame
with columns \code{level}, \code{file}, \code{field}, \code{message}.
}
\description{
Mirrors the checks documented in data-format.md and implemented on the
Python side by \code{validate()} in microplotpy -- keep both in sync.
}
