% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tax_fix.R
\name{mp_tax_fix}
\alias{mp_tax_fix}
\title{Fix unknown/missing taxonomy labels by falling back to the last known rank}
\usage{
mp_tax_fix(
  taxonomy,
  ranks = NULL,
  unknown_strings = c("", "na", "unknown", "unclassified", "uncultured", "unidentified",
    "metagenome"),
  label_format = "Unclassified_{last}"
)
}
\arguments{
\item{taxonomy}{A taxonomy data frame/tibble with \code{Feature_ID} and rank columns.}

\item{ranks}{Rank columns, high-to-low order. Default: auto-detected from
\code{c("Domain","Kingdom","Phylum","Class","Order","Family","Genus","Species")}
intersected with \code{names(taxonomy)}.}

\item{unknown_strings}{Values (case-insensitive, whitespace-trimmed) treated
as "unknown".}

\item{label_format}{Template for the fallback label; \code{{last}} is replaced
with the last known ancestor value.}
}
\value{
\code{taxonomy} with unknown values replaced.
}
\description{
Genus-level (or any rank) plots otherwise show blank/NA/"uncultured" bars,
which is common in 16S taxonomy tables and misleading in a stacked
barplot. This fills an unknown value at a given rank with the last known
ancestor rank so every feature still gets a meaningful label -- the same
idea as \code{tax_fix()} in the microViz package (Barnett et al. 2021,
Bioinformatics). Cascading unknowns (e.g. both Genus and Species missing)
all fall back to the same true ancestor rather than chaining onto each
other's fabricated labels.
}
