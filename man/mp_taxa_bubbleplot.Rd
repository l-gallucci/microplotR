% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bubbleplot.R
\name{mp_taxa_bubbleplot}
\alias{mp_taxa_bubbleplot}
\title{Taxa bubble (dot) plot}
\usage{
mp_taxa_bubbleplot(
  data,
  rank = "Genus",
  group_rank = "Phylum",
  top_n = 25,
  min_rel_abund = 0,
  min_prevalence = 0,
  detection = 0,
  facet_var = NULL,
  sample_order = NULL,
  fix_taxonomy = TRUE,
  tax_fix_ranks = NULL,
  max_size = 10
)
}
\arguments{
\item{data}{List with \code{feature_table}, \code{taxonomy}, \code{metadata}.}

\item{rank}{Taxonomy rank to plot (rows). Default \code{"Genus"}.}

\item{group_rank}{Upper taxonomy rank used to color and order \code{rank}.
Default \code{"Phylum"}. Set to \code{NULL} for a single-color plot.}

\item{top_n}{Number of \code{rank} taxa to show (ranked by total relative
abundance among taxa passing \code{min_rel_abund}/\code{min_prevalence}). Set
\code{NULL} to show every taxon that passes the filters, uncapped.}

\item{min_rel_abund}{Minimum mean relative abundance (\%) across all
samples for a taxon to be shown. \code{0} (default) disables this filter.}

\item{min_prevalence}{Minimum fraction of samples (\code{0-1}) -- or, if
\verb{> 1}, an absolute sample count -- in which a taxon must be detected
(relative abundance \verb{> detection}) to be shown. \code{0} (default) disables
this filter.}

\item{detection}{Relative-abundance (\%) threshold above which a taxon
counts as "detected" in a sample, for \code{min_prevalence}. Default \code{0}.}

\item{facet_var}{Optional metadata column to facet by.}

\item{sample_order}{Either \code{NULL}, a single metadata column name to sort
samples by, or an explicit character vector of \code{Sample_ID}.}

\item{fix_taxonomy}{Run \code{\link[=mp_tax_fix]{mp_tax_fix()}} on the taxonomy table first.}

\item{tax_fix_ranks}{Passed through to \code{\link[=mp_tax_fix]{mp_tax_fix()}}'s \code{ranks} argument.
\code{NULL} (default) auto-detects standard 16S rank columns; pass
explicitly when \code{data$taxonomy} is a non-taxonomic hierarchy.}

\item{max_size}{Maximum dot size (passed to \code{ggplot2::scale_size_area()}).}
}
\value{
A ggplot2 object.
}
\description{
One row per taxon at \code{rank} (top-N by total relative abundance), one
column per sample; dot size = relative abundance (\%), dot color = the
upper taxonomy level \code{group_rank} -- the \code{phyloseq::psmelt()} + ggplot
bubble / MicrobiomeAnalyst dot-plot convention. Every sample x taxon
combination is drawn (size 0 where absent) so presence/absence patterns
read directly off the grid. Taxa (rows) are ordered grouped by
\code{group_rank} block (blocks and within-block taxa both ordered by
descending total abundance), matching the ordering convention used by
\code{\link[=mp_taxa_barplot]{mp_taxa_barplot()}}.
}
\details{
By default (\code{fix_taxonomy = TRUE}), unknown/missing values at \code{rank} are
first resolved via \code{\link[=mp_tax_fix]{mp_tax_fix()}}.
}
