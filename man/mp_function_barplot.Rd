% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_function.R
\name{mp_function_barplot}
\alias{mp_function_barplot}
\title{Functional profile barplot}
\usage{
mp_function_barplot(
  feature_table,
  function_annotation,
  metadata = NULL,
  rank = "KEGG_ko",
  group_rank = "COG_category",
  top_n = 10,
  min_rel_abund = 0,
  min_prevalence = 0,
  detection = 0,
  facet_var = NULL,
  sample_order = NULL,
  fix_taxonomy = TRUE,
  nested_legend = TRUE,
  other_label = "Other"
)
}
\arguments{
\item{feature_table}{Per-sample gene/KO/pathway abundance table
(\code{Feature_ID} + one column per sample).}

\item{function_annotation}{\code{Feature_ID} + functional hierarchy columns
(e.g. eggNOG-mapper's \code{COG_category}, \code{KEGG_ko}, \code{Description}).}

\item{metadata}{Optional sample metadata (only needed for \code{facet_var}/
\code{sample_order} by metadata column); auto-stubbed from \code{feature_table}'s
sample columns if \code{NULL}.}

\item{rank}{Functional level to plot (bar segments). Default \code{"KEGG_ko"}.}

\item{group_rank}{Broader functional category used to group/color/order
\code{rank}. Default \code{"COG_category"}. Set \code{NULL} to disable (implies
\code{nested_legend = FALSE}).}

\item{top_n, min_rel_abund, min_prevalence, detection, facet_var, sample_order, fix_taxonomy, nested_legend, other_label}{See \code{\link[=mp_taxa_barplot]{mp_taxa_barplot()}} -- identical parameters and defaults.}
}
\value{
A ggplot2 object.
}
\description{
Stacked relative-abundance barplot for functional (not taxonomic) data --
gene family / KO / pathway abundance per sample, grouped/colored by a
broader functional category. Thin wrapper around \code{\link[=mp_taxa_barplot]{mp_taxa_barplot()}}:
identical engine, different input names and defaults suited to
functional-annotation tools.
}
\details{
Realistic input shape: a \code{feature_table} of per-sample gene/KO counts
(from any read quantifier, e.g. featureCounts/Salmon on predicted ORFs)
joined by \code{Feature_ID} to a \code{function_annotation} table from a gene
functional-annotation tool:
\itemize{
\item \strong{eggNOG-mapper} (\verb{*.emapper.annotations}): use \code{rank = "KEGG_ko"},
\code{group_rank = "COG_category"} (defaults) -- eggNOG-mapper's own column
names, unchanged.
\item \strong{KofamScan} (KO-only, no broader category): pass a \code{function_annotation}
with just \code{Feature_ID}/\code{KO}/\code{KO_definition}, set \code{rank = "KO"},
\code{group_rank = NULL}, \code{nested_legend = FALSE} (flat legend, no grouping
tier to nest under).
}

Missing/blank category values (e.g. a gene with no COG hit) are resolved
by \code{\link[=mp_tax_fix]{mp_tax_fix()}} the same way an unclassified genus is on the taxonomic
side -- see \code{fix_taxonomy}.
}
