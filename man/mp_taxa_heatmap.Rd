% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_heatmap.R
\name{mp_taxa_heatmap}
\alias{mp_taxa_heatmap}
\title{Clustered taxa heatmap}
\usage{
mp_taxa_heatmap(
  data,
  rank = "Genus",
  top_n = 25,
  min_rel_abund = 0,
  min_prevalence = 0,
  detection = 0,
  transform = c("clr", "log10"),
  pseudocount = NULL,
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  hclust_method = "average",
  show_dendrogram = TRUE,
  fix_taxonomy = TRUE,
  tax_fix_ranks = NULL
)
}
\arguments{
\item{data}{List with \code{feature_table}, \code{taxonomy}, \code{metadata}.}

\item{rank}{Taxonomy rank to plot (rows). Default \code{"Genus"}.}

\item{top_n}{Number of \code{rank} taxa to show (ranked by total relative
abundance among taxa passing \code{min_rel_abund}/\code{min_prevalence}). Set
\code{NULL} to show every taxon that passes the filters, uncapped.}

\item{min_rel_abund}{Minimum mean relative abundance (\%) across all
samples for a taxon to be shown. \code{0} (default) disables this filter.}

\item{min_prevalence}{Minimum fraction of samples (\code{0-1}) -- or, if
\verb{> 1}, an absolute sample count -- in which a taxon must be detected
(relative abundance \verb{> detection}) to be shown. \code{0} (default) disables
this filter.}

\item{detection}{Relative-abundance (\%) threshold above which a taxon
counts as "detected" in a sample, for \code{min_prevalence}. Default \code{0}.}

\item{transform}{\code{"clr"} (centered log-ratio, computed per sample across
the shown taxa) or \code{"log10"} (\code{log10(x + pseudocount)}).}

\item{pseudocount}{Added before log/CLR transform to handle zeros.}

\item{cluster_rows, cluster_cols}{Cluster taxa / samples.}

\item{hclust_method}{Agglomeration method passed to \code{\link[stats:hclust]{stats::hclust()}}.}

\item{show_dendrogram}{Draw dendrograms on the margins (requires at least
one of \code{cluster_rows}/\code{cluster_cols}).}

\item{fix_taxonomy}{Run \code{\link[=mp_tax_fix]{mp_tax_fix()}} on the taxonomy table first.}

\item{tax_fix_ranks}{Passed through to \code{\link[=mp_tax_fix]{mp_tax_fix()}}'s \code{ranks} argument.
\code{NULL} (default) auto-detects standard 16S rank columns; pass
explicitly when \code{data$taxonomy} is a non-taxonomic hierarchy (e.g. a
functional annotation table).}
}
\value{
A ggplot2 object (\code{show_dendrogram = FALSE}) or a patchwork object
(\code{show_dendrogram = TRUE}).
}
\description{
Top-N taxa at \code{rank} x samples, CLR- or log10-transformed relative
abundance, with hierarchical clustering (UPGMA) on both axes -- samples
clustered by Bray-Curtis dissimilarity on relative abundance (the
community-ecology standard, e.g. \code{vegan::vegdist}), taxa clustered by
Euclidean distance on the transformed values shown -- matching the
\code{ampvis2::amp_heatmap} / \code{pheatmap} / \code{ComplexHeatmap} convention for
marker-gene heatmaps. Dendrograms are drawn on both margins by default
(\code{show_dendrogram = TRUE}); set to \code{FALSE} for a plain ordered tile grid.
}
\details{
By default (\code{fix_taxonomy = TRUE}), unknown/missing values at \code{rank} are
first resolved via \code{\link[=mp_tax_fix]{mp_tax_fix()}}.
}
