% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_mag_quality.R
\name{mp_mag_quality_plot}
\alias{mp_mag_quality_plot}
\title{MAG (metagenome-assembled genome) quality plot}
\usage{
mp_mag_quality_plot(
  mag_table,
  size_col = "Genome_Size",
  color_col = NULL,
  show_mimag_thresholds = TRUE
)
}
\arguments{
\item{mag_table}{A data frame with a bin-identifier column (\code{Name} or
CheckM1's \code{"Bin Id"}), \code{Completeness}, \code{Contamination}, and optionally
a genome-size column (\code{Genome_Size} or CheckM1's \code{"Genome size (bp)"}).}

\item{size_col}{Column to map to dot size (e.g. genome size). \code{NULL}
disables sizing (uniform dots). Default \code{"Genome_Size"} (silently
ignored if not present in \code{mag_table}).}

\item{color_col}{Optional column to map to dot color (e.g. a taxon or
group column).}

\item{show_mimag_thresholds}{Draw the MIMAG quality-tier reference lines
and shaded high-quality region.}
}
\value{
A ggplot2 object.
}
\description{
One dot per MAG/bin, x = Contamination (\%), y = Completeness (\%) -- the
standard first figure in any MAG-recovery paper. Accepts CheckM2's
\code{quality_report.tsv} or CheckM (v1)'s \code{qa}/\code{bin_stats} table directly
(column-name differences between the two tools are normalized
internally). Draws dashed reference lines and a shaded region at the
MIMAG quality-tier thresholds by default (Bowers et al. 2017, \emph{Nature
Biotechnology} -- "Minimum information about a single amplified genome
and a metagenome-assembled genome"): high quality >=90\% completeness &
<5\% contamination; medium quality >=50\% completeness & <10\% contamination.
}
