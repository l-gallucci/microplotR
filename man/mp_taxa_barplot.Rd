% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_barplot.R
\name{mp_taxa_barplot}
\alias{mp_taxa_barplot}
\title{Stacked relative-abundance taxa barplot, optionally with a nested legend}
\usage{
mp_taxa_barplot(
  data,
  rank = "Genus",
  group_rank = "Phylum",
  top_n = 10,
  min_rel_abund = 0,
  min_prevalence = 0,
  detection = 0,
  facet_var = NULL,
  sample_order = NULL,
  fix_taxonomy = TRUE,
  tax_fix_ranks = NULL,
  nested_legend = TRUE,
  other_label = "Other",
  long = NULL
)
}
\arguments{
\item{data}{List with \code{feature_table}, \code{taxonomy}, \code{metadata} (as from
\code{\link[=mp_read_data]{mp_read_data()}}).}

\item{rank}{Taxonomy rank to plot (bar segments). Default \code{"Genus"}.}

\item{group_rank}{Upper taxonomy rank used to group/color/order \code{rank}
and (if \code{nested_legend = TRUE}) to build the bold legend headers.
Default \code{"Phylum"}. Set to \code{NULL} to disable grouping entirely (implies
\code{nested_legend = FALSE}).}

\item{top_n}{Number of \code{rank} taxa to show individually (ranked by total
relative abundance among taxa passing \code{min_rel_abund}/\code{min_prevalence});
the rest are pooled into \code{other_label}. Set \code{NULL} to show every taxon
that passes the filters, uncapped.}

\item{min_rel_abund}{Minimum mean relative abundance (\%) across all
samples for a taxon to be eligible; taxa below this are pooled into
\code{other_label} regardless of \code{top_n}. \code{0} (default) disables this filter.}

\item{min_prevalence}{Minimum fraction of samples (\code{0-1}) -- or, if
\verb{> 1}, an absolute sample count -- in which a taxon must be detected
(relative abundance \verb{> detection}) to be eligible. \code{0} (default)
disables this filter.}

\item{detection}{Relative-abundance (\%) threshold above which a taxon
counts as "detected" in a sample, for \code{min_prevalence}. Default \code{0}.}

\item{facet_var}{Optional metadata column to facet by (e.g. a treatment
group), one panel per level.}

\item{sample_order}{Either \code{NULL} (keep the sample order from
\code{feature_table}), a single metadata column name to sort samples by, or
an explicit character vector of \code{Sample_ID} in the desired order.}

\item{fix_taxonomy}{Run \code{\link[=mp_tax_fix]{mp_tax_fix()}} on the taxonomy table first.}

\item{tax_fix_ranks}{Passed through to \code{\link[=mp_tax_fix]{mp_tax_fix()}}'s \code{ranks} argument.
\code{NULL} (default) auto-detects standard 16S rank columns; pass
explicitly (e.g. \code{c(group_rank, rank)}) when \code{data$taxonomy} is a
non-taxonomic hierarchy (e.g. a functional annotation table).}

\item{nested_legend}{Use the grouped/bold-header legend. Requires
\code{group_rank}.}

\item{other_label}{Label for the pooled "everything else" bucket (taxa
failing \code{min_rel_abund}/\code{min_prevalence}, or outside \code{top_n}).}

\item{long}{Advanced: a precomputed long-format table (same
shape \code{.mp_long_abundance()} returns) to reuse instead of re-melting/
rejoining \code{data} from scratch -- e.g. a Shiny app caching one melt per
upload and reusing it across rank/top_n/plot-type tweaks. \code{NULL}
(default) derives it from \code{data} as before; \code{fix_taxonomy}/
\code{tax_fix_ranks} are ignored if \code{long} is supplied (assumed already
applied).}
}
\value{
A ggplot2 object.
}
\description{
Standard 16S/marker-gene stacked barplot: one bar per sample, segments are
relative abundance (\%) of the top-N taxa at \code{rank} (everything else pooled
into \code{other_label}). With \code{nested_legend = TRUE} (default), taxa are
ordered and colored grouped by \code{group_rank} -- each group gets a base hue,
its member taxa get shades of that hue, and the legend shows a bold
\verb{**group_rank**} header above the first taxon of each group (rendered via
\code{ggtext::element_markdown()}), matching the grouped-legend convention used
e.g. in the \code{ggnested} package for microbiome barplots. Bars/legend are
ordered by abundance at \code{rank} (default Genus), grouped by \code{group_rank}
(default Phylum) abundance.
}
\details{
By default (\code{fix_taxonomy = TRUE}), unknown/missing values at \code{rank} are
first resolved via \code{\link[=mp_tax_fix]{mp_tax_fix()}} so no bar segment is left unlabeled.

The plot carries no title/subtitle -- only axis titles and the legend --
so it's ready to drop into a figure panel as-is.
}
