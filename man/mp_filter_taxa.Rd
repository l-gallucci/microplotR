% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_taxa.R
\name{mp_filter_taxa}
\alias{mp_filter_taxa}
\title{Filter taxa by minimum abundance and/or prevalence}
\usage{
mp_filter_taxa(
  agg,
  taxon_col,
  min_rel_abund = 0,
  min_prevalence = 0,
  detection = 0
)
}
\arguments{
\item{agg}{A per-(Sample_ID, taxon) aggregated relative-abundance table
(as built internally by the plot functions), with columns \code{Sample_ID},
\code{taxon_col}, \code{rel_abund}. Every taxon must have one row per sample
(0 where absent) for prevalence/mean to be computed correctly.}

\item{taxon_col}{Name of the taxon column in \code{agg}.}

\item{min_rel_abund}{Minimum mean relative abundance (\%) across all
samples. \code{0} (default) disables this filter.}

\item{min_prevalence}{Minimum fraction of samples (\code{0-1}) -- or, if a
value \verb{> 1} is given, an absolute sample count -- in which the taxon
must be "detected" (see \code{detection}). \code{0} (default) disables this
filter.}

\item{detection}{Relative-abundance (\%) threshold above which a taxon
counts as "detected" in a sample, for \code{min_prevalence} purposes.
Default \code{0} (any nonzero abundance counts as present).}
}
\value{
Character vector of taxa (from \code{taxon_col}) passing both filters.
}
\description{
Shared by \code{\link[=mp_taxa_barplot]{mp_taxa_barplot()}}, \code{\link[=mp_taxa_heatmap]{mp_taxa_heatmap()}}, \code{\link[=mp_taxa_bubbleplot]{mp_taxa_bubbleplot()}}.
Narrows the candidate taxa \emph{before} \code{top_n} is applied: \code{top_n} then picks
the most abundant among survivors, so a taxon is excluded from the "top"
set either because it failed a threshold here or because it wasn't in the
top N of what passed -- both cases are treated identically by the calling
plot function (pooled into \code{other_label} for the barplot, simply dropped
for the heatmap/bubbleplot).
}
