% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_treemap.R
\name{mp_taxa_treemap}
\alias{mp_taxa_treemap}
\title{Taxa (or functional) treemap}
\usage{
mp_taxa_treemap(
  data,
  rank = "Genus",
  group_rank = "Phylum",
  top_n = NULL,
  min_rel_abund = 0,
  min_prevalence = 0,
  detection = 0,
  fix_taxonomy = TRUE,
  tax_fix_ranks = NULL,
  other_label = "Other"
)
}
\arguments{
\item{data}{List with \code{feature_table}, \code{taxonomy}, \code{metadata}.}

\item{rank}{Taxonomy/functional level to plot (rectangles). Default \code{"Genus"}.}

\item{group_rank}{Upper level used to group/color \code{rank} and draw subgroup
borders. Default \code{"Phylum"}. Set \code{NULL} to color by \code{rank} directly
instead (no subgroup borders).}

\item{top_n}{Number of \code{rank} taxa to show individually (ranked by mean
relative abundance among taxa passing \code{min_rel_abund}/\code{min_prevalence});
the rest are pooled into \code{other_label}. \code{NULL} (default) shows every
taxon that passes the filters, uncapped.}

\item{min_rel_abund, min_prevalence, detection}{See \code{\link[=mp_taxa_barplot]{mp_taxa_barplot()}}.}

\item{fix_taxonomy}{Run \code{\link[=mp_tax_fix]{mp_tax_fix()}} on the taxonomy table first.}

\item{tax_fix_ranks}{Passed through to \code{\link[=mp_tax_fix]{mp_tax_fix()}}'s \code{ranks} argument;
see \code{\link[=mp_taxa_barplot]{mp_taxa_barplot()}}.}

\item{other_label}{Label for the pooled "everything else" bucket.}
}
\value{
A ggplot2 object.
}
\description{
Hierarchical composition as nested rectangles -- a static, publication-
ready alternative to an interactive Krona chart: area = relative
abundance (mean across samples, so areas sum to ~100\%), grouped/colored
by \code{group_rank} with a subgroup border, each rectangle labeled directly
(no external legend needed). Works generically on taxonomic or functional
data (see \code{\link[=mp_function_barplot]{mp_function_barplot()}} for the same generalization on the
barplot/heatmap).
}
\details{
By default (\code{fix_taxonomy = TRUE}), unknown/missing values at \code{rank} are
first resolved via \code{\link[=mp_tax_fix]{mp_tax_fix()}}.
}
