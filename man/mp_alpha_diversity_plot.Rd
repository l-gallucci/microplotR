% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alpha_diversity.R
\name{mp_alpha_diversity_plot}
\alias{mp_alpha_diversity_plot}
\title{Alpha diversity boxplot}
\usage{
mp_alpha_diversity_plot(
  data,
  group_var,
  metrics = c("Observed", "Shannon", "Simpson"),
  test = "auto"
)
}
\arguments{
\item{data}{List with \code{feature_table}, \code{taxonomy}, \code{metadata}.}

\item{group_var}{Metadata column (categorical, >=2 levels) to compare.}

\item{metrics}{Diversity metrics to compute/plot, any of \code{"Observed"},
\code{"Shannon"}, \code{"Simpson"}. Default all three.}

\item{test}{\code{"auto"} (default: Wilcoxon for 2 groups, Kruskal-Wallis for
more than 2), \code{"wilcoxon"}, \code{"kruskal"}, or \code{NULL} to disable the test
annotation.}
}
\value{
A ggplot2 object, faceted by metric.
}
\description{
Observed richness, Shannon, and Simpson diversity per sample (via
\code{vegan::specnumber()}/\code{vegan::diversity()}), boxplot + jitter by a
grouping metadata variable, faceted one panel per metric, with a
Wilcoxon (2 groups) or Kruskal-Wallis (>2 groups) test annotated via
\code{ggpubr::stat_compare_means()} -- the standard alpha-diversity comparison
figure in amplicon/marker-gene studies.
}
