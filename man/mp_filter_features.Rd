% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_features.R
\name{mp_filter_features}
\alias{mp_filter_features}
\title{Filter features (ASVs/OTUs) from a MicrobiomeData object}
\usage{
mp_filter_features(
  data,
  exclude_taxa = NULL,
  exclude_ranks = NULL,
  exclude_features = NULL,
  min_total_count = 0,
  remove_singletons = FALSE
)
}
\arguments{
\item{data}{List with \code{feature_table}, \code{taxonomy}, \code{metadata} (as from
\code{\link[=mp_read_data]{mp_read_data()}}).}

\item{exclude_taxa}{Character vector of taxon names to drop wherever
they appear (exact match, case/whitespace-insensitive) in any of
\code{exclude_ranks} -- e.g. \code{c("Chloroplast", "Mitochondria")} to remove
the classic 16S plastid/organelle contaminants regardless of which
rank column a given classifier put them in.}

\item{exclude_ranks}{Which \code{data$taxonomy} columns to check for
\code{exclude_taxa} matches. \code{NULL} (default) checks every column except
\code{Feature_ID}.}

\item{exclude_features}{Character vector of exact \code{Feature_ID} values
to drop directly, regardless of taxonomy (e.g. a denoiser's own
contaminant/chimera flag list).}

\item{min_total_count}{Minimum total read count (summed across all
samples) a feature must have to be kept. \code{0} (default) disables this
filter.}

\item{remove_singletons}{If \code{TRUE}, also drop features with a total
read count of exactly 1 (the classic amplicon "singleton") --
equivalent to raising \code{min_total_count} to at least \code{2}.}
}
\value{
A list with:
\itemize{
\item \code{data}: the filtered \code{feature_table}/\code{taxonomy} (same shape/columns,
\code{metadata} untouched), ready for \code{\link[=mp_validate]{mp_validate()}} or the plot
functions.
\item \code{summary}: a tibble, one row per active filter reason plus a
\code{"total"} row (removed by any reason, i.e. not simply the sum, so
features hit by more than one reason aren't double-counted), with
\code{n_features_removed}/\code{pct_features_removed} and
\code{n_reads_removed}/\code{pct_reads_removed}. Percentages are always
against the \emph{original} (pre-filter) totals, so they reflect what
filtering actually took out of the input.
}
}
\description{
Removes whole features from \code{feature_table}/\code{taxonomy} before any
plot-level aggregation -- e.g. dropping chloroplast/mitochondrial
contaminants, singleton ASVs, or an explicit exclude list -- and
reports how much was removed (feature count and read count, in
absolute terms and percentage) so filtering decisions aren't made
blind. Complements \code{\link[=mp_filter_taxa]{mp_filter_taxa()}}, which filters \emph{after}
aggregation to a chosen rank for a specific plot; this one operates on
raw features and feeds a cleaned dataset into \code{\link[=mp_validate]{mp_validate()}} or the
plot functions.
}
