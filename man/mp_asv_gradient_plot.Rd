% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gradient.R
\name{mp_asv_gradient_plot}
\alias{mp_asv_gradient_plot}
\title{ASV/taxon abundance over a continuous gradient}
\usage{
mp_asv_gradient_plot(
  data,
  gradient_var,
  rank = "Genus",
  group_rank = "Phylum",
  top_n = 10,
  taxa = NULL,
  min_rel_abund = 0,
  min_prevalence = 0,
  detection = 0,
  facet = FALSE,
  smooth = TRUE,
  smooth_method = "loess",
  fix_taxonomy = TRUE,
  tax_fix_ranks = NULL,
  other_label = "Other",
  long = NULL
)
}
\arguments{
\item{data}{List with \code{feature_table}, \code{taxonomy}, \code{metadata}.}

\item{gradient_var}{Metadata column holding the continuous gradient
(e.g. \code{"Depth_m"}, \code{"pH"}). Must be numeric.}

\item{rank}{Taxonomy rank to plot. Default \code{"Genus"}.}

\item{group_rank}{Upper taxonomy rank used to group/color/order \code{rank}
(nested-legend convention, see \code{\link[=mp_taxa_barplot]{mp_taxa_barplot()}}). Default \code{"Phylum"}.
Set \code{NULL} for a flat legend.}

\item{top_n}{Number of \code{rank} taxa to show (ranked by total relative
abundance among taxa passing \code{min_rel_abund}/\code{min_prevalence}); the
rest are pooled into \code{other_label}. \code{NULL} shows every taxon that
passes the filters, uncapped. Ignored if \code{taxa} is given.}

\item{taxa}{Optional character vector of exact \code{rank} values to show
(e.g. \code{"Escherichia"} for a single taxon's gradient, or a short
explicit list) -- bypasses \code{top_n}/\code{min_rel_abund}/\code{min_prevalence}
ranking entirely and shows exactly these taxa (any not present in the
data are silently ignored). \code{NULL} (default) falls back to the
\code{top_n}-ranked selection.}

\item{min_rel_abund, min_prevalence, detection}{See \code{\link[=mp_taxa_barplot]{mp_taxa_barplot()}}.}

\item{facet}{Facet into one panel per taxon (small multiples) instead of
overlaying all taxa in one panel with a color legend.}

\item{smooth}{Add a per-taxon trend line (\code{ggplot2::geom_smooth()}).}

\item{smooth_method}{Smoothing method passed to \code{geom_smooth()}. Default
\code{"loess"}.}

\item{fix_taxonomy}{Run \code{\link[=mp_tax_fix]{mp_tax_fix()}} on the taxonomy table first.}

\item{tax_fix_ranks}{Passed through to \code{\link[=mp_tax_fix]{mp_tax_fix()}}'s \code{ranks} argument;
see \code{\link[=mp_taxa_barplot]{mp_taxa_barplot()}}.}

\item{other_label}{Label for the pooled "everything else" bucket.}

\item{long}{Advanced: a precomputed long-format table (same shape
\code{.mp_long_abundance()} returns) to reuse instead of re-melting/rejoining
\code{data} from scratch. \code{NULL} (default) derives it from \code{data} as before;
\code{fix_taxonomy}/\code{tax_fix_ranks} are ignored if \code{long} is supplied.}
}
\value{
A ggplot2 object.
}
\description{
Relative abundance (\%) of the top-N taxa at \code{rank} plotted against a
continuous metadata gradient (depth, pH, time...) -- the standard way to
show which taxa respond to an environmental gradient (e.g.
\code{phyloseq::psmelt()} + \code{ggplot(aes(x = gradient, y = Abundance, color = Taxon))}, common in depth-profile / time-series microbiome papers).
}
\details{
Same taxa filtering (\code{top_n}/\code{min_rel_abund}/\code{min_prevalence}/\code{detection})
and nested-legend grouping (\code{group_rank}) conventions as
\code{\link[=mp_taxa_barplot]{mp_taxa_barplot()}}, so a barplot and gradient plot of the same data
select/color/order taxa consistently.

By default (\code{fix_taxonomy = TRUE}), unknown/missing values at \code{rank} are
first resolved via \code{\link[=mp_tax_fix]{mp_tax_fix()}}.
}
