% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_function.R
\name{mp_function_heatmap}
\alias{mp_function_heatmap}
\title{Functional profile heatmap}
\usage{
mp_function_heatmap(
  feature_table,
  function_annotation,
  rank = "KEGG_ko",
  top_n = 25,
  min_rel_abund = 0,
  min_prevalence = 0,
  detection = 0,
  transform = "clr",
  pseudocount = NULL,
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  hclust_method = "average",
  show_dendrogram = TRUE,
  fix_taxonomy = TRUE
)
}
\arguments{
\item{feature_table}{Per-sample gene/KO/pathway abundance table
(\code{Feature_ID} + one column per sample).}

\item{function_annotation}{\code{Feature_ID} + functional hierarchy columns
(e.g. eggNOG-mapper's \code{COG_category}, \code{KEGG_ko}, \code{Description}).}

\item{rank}{Functional level to plot (rows). Default \code{"KEGG_ko"}.}

\item{top_n, min_rel_abund, min_prevalence, detection, transform, pseudocount, cluster_rows, cluster_cols, hclust_method, show_dendrogram, fix_taxonomy}{See \code{\link[=mp_taxa_heatmap]{mp_taxa_heatmap()}} -- identical parameters and defaults.}
}
\value{
A ggplot2 object (\code{show_dendrogram = FALSE}) or a patchwork object.
}
\description{
Clustered heatmap for functional (not taxonomic) data. Thin wrapper
around \code{\link[=mp_taxa_heatmap]{mp_taxa_heatmap()}} -- identical engine, different input names and
defaults suited to functional-annotation tools (eggNOG-mapper/KofamScan,
see \code{\link[=mp_function_barplot]{mp_function_barplot()}}).
}
